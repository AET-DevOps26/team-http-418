apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: client
  ports:
    - port: {{ .Values.client.port }}
      targetPort: {{ .Values.client.port }}
  type: ClusterIP
