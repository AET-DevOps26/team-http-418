# Example only — replace values before applying. Use Helm chart for production.
apiVersion: v1
kind: Secret
metadata:
  name: aidan-secret
  namespace: aidan
type: Opaque
stringData:
  DB_PASS: "changeme"
  SPRING_DATASOURCE_PASSWORD: "changeme"
